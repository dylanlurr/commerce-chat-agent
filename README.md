# commerce-chat-agent

Python/FastAPI commerce chat project with separated services:

- `merchant-backend`: CSV upload + Postgres product catalog service.
- `consumer-backend`: AI chat service using OpenAI function-calling tools for search, cart, compare, and checkout.
- `frontend`: frontend application folder.
